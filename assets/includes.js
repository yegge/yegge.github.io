// /assets/includes.jsasync function inject(selector, path) {  const host = document.querySelector(selector);  if (!host) return;  try {    const res = await fetch(path, { cache: 'no-store' });    if (!res.ok) throw new Error(res.status + ' ' + res.statusText);    host.innerHTML = await res.text();    if (selector === '#site-header') bindDropdowns();  } catch (err) {    console.error('Include failed:', path, err);  }}function bindDropdowns() {  const buttons = document.querySelectorAll('[data-dropdown]');  const panels  = document.querySelectorAll('[data-panel]');  const closeAll = () => {    panels.forEach(p => p.style.display = 'none');    buttons.forEach(b => b.setAttribute('aria-expanded', 'false'));  };  buttons.forEach(btn => {    btn.addEventListener('click', (e) => {      const key = btn.getAttribute('data-dropdown');      const panel = document.querySelector(`[data-panel="${key}"]`);      const open = panel && panel.style.display !== 'none';      closeAll();      if (panel && !open) {        panel.style.display = 'block';        btn.setAttribute('aria-expanded', 'true');      }      e.stopPropagation();    });  });  document.addEventListener('click', closeAll);  document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeAll(); });}(function init(){  inject('#site-header', '/header.html');  inject('#site-footer', '/footer.html');})();(() => {  const form = document.getElementById('subscribe');  if (!form) return;  // Worker endpoint:  const SUBSCRIBE_URL = "https://likeaghost-subscribe.<your-subdomain>.workers.dev/subscribe"; // or https://api.yourdomain/subscribe  const msg = document.getElementById('s-msg');  form.addEventListener('submit', async (e) => {    e.preventDefault();    msg.textContent = "Working…";    const data = new FormData(form);    const email = (data.get('email') || '').toString().trim();    const hp = (data.get('hp') || '').toString().trim();    try {      const res = await fetch(SUBSCRIBE_URL, {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ email, hp })      });      const j = await res.json().catch(() => ({}));      if (!res.ok) throw new Error(j.error || res.statusText);      msg.textContent = "Check your email to confirm your subscription.";      form.reset();    } catch (err) {      msg.textContent = "Subscription failed. Try again.";      console.error(err);    }  });})();